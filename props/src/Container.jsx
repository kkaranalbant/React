import React from "react";


function Container (props) {
    return (<div>
        Container
        {props.children}
        {props.children}
        {props.children}
        {props.children}
    </div>);
}

export default Container ;